import SingleContact from "./SingleContact";

const ContactView = (props) => {

    return (
        <div>
            <SingleContact/>
            <SingleContact/>
        </div>
    );
}

export default ContactView;